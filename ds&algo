Given an array and a target, return all unique pairs [a, b] such that:
nums = [1, 2, 3, 2, 3, 4]
target = 5
[[2, 3], [1, 4]]  Even though (2,3) appears multiple times, return it only once
nums = [-1, 0, 1, 2, -1, -4]
target = 0
[[-1, 1]]
public List<List<Integer>> twoSumAll(int[] nums, int target) {
    Set<Integer> seen = new HashSet<>();
    Set<List<Integer>> result = new HashSet<>();

    for (int num : nums) {
        int complement = target - num;
        if (seen.contains(complement)) {
            List<Integer> pair = Arrays.asList(Math.min(num, complement), Math.max(num, complement));
            result.add(pair);
        }
        seen.add(num);
    }
    return new ArrayList<>(result);
}

nums = [1, 2, 3, 2, 3, 4]
target = 5


Find the index of a target element.
nums = [4,5,6,7,0,1,2]
target = 0

public int search(int[] nums, int target) {  ((O(log n))
    int low = 0, high = nums.length - 1;

    while (low <= high) {
        int mid = low + (high - low) / 2;

        if (nums[mid] == target) return mid;

        // Left half is sorted
        if (nums[low] <= nums[mid]) {
            if (nums[low] <= target && target < nums[mid]) {
                high = mid - 1;
            } else {
                low = mid + 1;
            }
        } 
        // Right half is sorted
        else {
            if (nums[mid] < target && target <= nums[high]) {
                low = mid + 1;
            } else {
                high = mid - 1;
            }
        }
    }

    return -1;
}
